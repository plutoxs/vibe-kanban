{
  "name": "lite-kanban",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:server": "pnpm --filter @lite-kanban/server run dev",
    "dev:web": "pnpm --filter @lite-kanban/web run dev",
    "build": "pnpm -r run build",
    "check": "pnpm -r run check"
  }
}
